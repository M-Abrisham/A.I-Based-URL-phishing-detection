# Sample emails

Public, redacted `.eml` files used for the README demo and screenshots.

Unlike `tests/fixtures/`, these are meant to be realistic — drawn from public
phishing feeds (e.g. PhishTank dumps) with PII and recipient addresses
scrubbed.

**Never commit real recipient addresses, credentials, or live payloads.**
